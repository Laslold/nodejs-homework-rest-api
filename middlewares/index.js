const validateBody = require("./validateBody");
const isValid = require("./isValidId");
const authenticate = require("./authenticate");
// const auth = require("./auth");
module.exports = { validateBody, isValid, authenticate };
